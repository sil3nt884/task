import styled from "styled-components";

export const Text = styled.p`
          inline-size: 120px;
          overflow-wrap: break-word;
    `

export const DinnerText = styled.p`
          inline-size: 116px;
          overflow-wrap: break-word;
    `
